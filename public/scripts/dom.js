const button = $('#button')
const main = $('main')
const body = $('body')
const close = $('#close')
const label = $('#lb-phone')

const nome = $('#nome')
const email = $('#email')
const phone = $("#phone")
const submit = $('#submit')
const reset = $('#reset')

export { button, main, body, close, phone, label, nome, email, submit, reset }